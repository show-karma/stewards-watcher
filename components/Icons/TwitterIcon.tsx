/* eslint-disable id-length */
import { Icon, IconProps } from '@chakra-ui/react';
import { BsTwitter } from 'react-icons/bs';

export const TwitterIcon = (props: IconProps) => (
  <Icon as={BsTwitter} {...props} />
);
