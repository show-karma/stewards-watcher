/* eslint-disable id-length */
import { Icon, IconProps } from '@chakra-ui/react';
import { FaDiscord } from 'react-icons/fa';

export const DiscordIcon = (props: IconProps) => (
  <Icon as={FaDiscord} {...props} />
);
