/* eslint-disable id-length */
import { Icon, IconProps } from '@chakra-ui/react';
import { CgWebsite } from 'react-icons/cg';

export const WebsiteIcon = (props: IconProps) => (
  <Icon as={CgWebsite} {...props} />
);
